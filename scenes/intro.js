import { k } from "/kaboom.js";

k.scene("intro", () => {
  k.add([
    // TODO - add click to play here, necessary to get interaction from player
    // so that all sounds can be played
    k.text("intro scene"),
  ]);
});